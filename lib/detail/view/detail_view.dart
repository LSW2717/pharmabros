import 'package:flutter/cupertino.dart';

class DetailView extends StatelessWidget {
  const DetailView({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('디테일페이지입니다.'),);
  }
}
